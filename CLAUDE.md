# CLAUDE.md

Authoritative instructions for agents in this repo live in `AGENTS.md` at the repo root — read that file, not this one, for build/test commands, the contribution protocol, and style rules.
This pointer file exists only because Claude Code looks for `CLAUDE.md` by convention (PRD §4 Stage 2: "mirrored/linked as CLAUDE.md for Claude Code"); it carries no separate instructions of its own.
